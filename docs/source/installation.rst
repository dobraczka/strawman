Installation
============

The easiest way to get strawman is via pip:

.. code-block:: bash

   pip install strawman

To build strawman from source use `poetry <https://python-poetry.org/>`_ 

.. code-block:: bash

   git clone git@github.com:dobraczka/strawman.git 
   cd strawman 
   poetry install
